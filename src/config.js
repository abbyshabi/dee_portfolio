const config = {
  
    service_id: {
      id: process.env.REACT_APP_SERVICE_ID
    },
    template_id: {
      id: process.env.REACT_APP_TEMPLATE_ID
    },
    user_id: {
      id: process.env.REACT_APP_USER_ID
    }
  };
  
  export default config;
  